#ifndef CLASS_Q1_H
#define CLASS_Q1_H
#include "dossier.h"
#include "UV.h"
using namespace std;
//using namespace UV_credits_types;
//using namespace question3;


/* en partie dans UV.h de nicolas
class UV {
    string code;
    string titre;
    unsigned int nbCredits;
    Categorie categorie;
    UV(const string& c, const string& t, unsigned int nbc, Categorie cat):
      code(c),titre(t),nbCredits(nbc),categorie(cat){}
    UV(const UV & uv); //à implémenter plus tard ;)
    UV& operator=(const UV& uv);
    friend class UVManager;
public:

    string getCode() const { return code; }
    string getTitre() const { return titre; }
    unsigned int getNbCredits() const { return nbCredits; }
    Categorie getCategorie() const { return categorie; }
};
    ostream& operator<<(ostream& f, const UV& uv);
*/


    class Formation{
    };

#endif // CLASS_Q1_H
